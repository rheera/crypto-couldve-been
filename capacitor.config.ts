import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ramanheera.crypto.couldve.been",
  appName: "crypto-couldve-been",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
